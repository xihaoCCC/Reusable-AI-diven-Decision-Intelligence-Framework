# Tests

The active test suite verifies the CTDC-informed anti-trafficking prototype:

- cleaned HTCDS and HTCDS+ schema loading and validation
- CTDC-to-HTCDS+ coverage and extension reporting
- synthetic CTDC-style record generation
- standardized data mapping and separate model-feature selection
- exploitation-type probability scoring
- configurable triage scenarios
- human-review queue table creation
